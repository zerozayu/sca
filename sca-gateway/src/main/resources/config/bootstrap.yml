spring:
  application:
    name: sca-gateway
  profiles:
    active: dev

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: dev
        group: sca
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: dev
        group: sca
        file-extension: yml

